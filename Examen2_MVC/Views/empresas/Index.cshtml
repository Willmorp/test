@model IEnumerable<Examen2_MVC.Models.empresa>

<div class="row">
    <div class="col-lg-12">
        <div class="box">
            <div class="box-header">
                <h4>Persona</h4>
            </div>
            <div class="box-body">
                @Ajax.ActionLink("Crear Empresa", "Create", new AjaxOptions {
                HttpMethod="GET",
                InsertionMode = InsertionMode.Replace,
                UpdateTargetId = "contenido_modal",
                OnSuccess = "abrirModal('#modal_crear_empresa')",
                })
                <div id="listar_empresa">
                    @{Html.RenderPartial("List", Model);}
                </div>
            </div>

        </div>
    </div>
</div>
<div id="contenido_modal"></div>