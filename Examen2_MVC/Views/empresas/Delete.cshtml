@model Examen2_MVC.Models.empresa



<div class="modal fade" id="modal_eliminar_empresa">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4>Estas Seguro De Eliminar Esta Empresa</h4>
            </div>
            @using (Ajax.BeginForm("Delete", "Empresas", new AjaxOptions()
            {
                HttpMethod = "POST",
                UpdateTargetId = "listar_empresa",
                OnSuccess = "mensajeCrear('#modal_eliminar_empresa')"
            }))
            {
                <div class="modal-body">



                    @Html.AntiForgeryToken()
                    @Html.DisplayFor(model => Model.nombreempresa)
                    @Html.HiddenFor(model => Model.idempresa)
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline pull-left" data-dismiss="modal">Cerrar</button>
                    <button type="submit" class="btn btn-outline">Eliminar</button>
                </div>
            }

        </div>

    </div>
</div>