@model IEnumerable<Examen2_MVC.Models.usuario>

<div class="row">
    <div class="col-lg-12">
        <div class="box">
            <div class="box-header">
                <h4>Usuario</h4>
            </div>
            <div class="box-body">
                @Ajax.ActionLink("Crear Usuario", "Create", new AjaxOptions {
                HttpMethod="GET",
                InsertionMode = InsertionMode.Replace,
                UpdateTargetId = "contenido_modal",
                OnSuccess = "abrirModal('#modal_crear_usuario')",
                })
                <div id="listar_usuario">
                    @{Html.RenderPartial("List", Model);}
                </div>
            </div>

        </div>
    </div>
</div>
<div id="contenido_modal"></div>